---
# ==============================================================
# ARQUIVO: roles/monitoring/handlers/main.yml
# MÓDULO 4 - Projeto Final
# ==============================================================

- name: "Reiniciar node_exporter"
  ansible.builtin.systemd:
    name: node_exporter
    state: restarted

- name: "Recarregar systemd monitoring"
  ansible.builtin.systemd:
    daemon_reload: true
